import suprsend from "@suprsend/web-sdk";
import dotenv from 'dotenv';
dotenv.config();

// suprsend.init(process.env.SUPRSEND_WORKSPACE_KEY, process.env.SUPRSEND_WORKSPACE_SECRET);

// suprsend.identify("vipin123");
// suprsend.user.add_email("vipin@gmail.com");
// suprsend.user.add_sms("7318195685");

// suprsend.user.set({"name":"Vipin", "age": "21"});